/** Public API for validating and mapping the GitHub Actions artifact. */
export {
  canonicalEvidenceFromGithub,
  parseGithubEvidence,
  type GithubEvidenceDocument,
  type GithubContractEvidenceOptions,
  type GithubRunContext,
} from './github-evidence.js';
