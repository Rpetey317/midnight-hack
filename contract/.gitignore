node_modules/

# Generated contract JS and declarations are committed because the parser,
# runtime, and UI build without requiring the Compact compiler.
#
# Proving keys (15 MB) and ZKIR stay out — regenerate with `npm run compile:keys`.
src/managed/audit_registry/keys/
src/managed/audit_registry/zkir/

# Encrypted private-state store (the runtime normally keeps this under
# ~/.zkuat/runtime instead).
midnight-level-db/
