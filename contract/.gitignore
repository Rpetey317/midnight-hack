node_modules/

# Compiler output — contract JS, ZKIR, and proving keys. Regenerate with
# `npm run compile` (fast, no keys) or `npm run compile:keys` (real keys).
src/managed/

# Encrypted private-state store written by levelPrivateStateProvider.
midnight-level-db/

# Devnet deployment record written by `npm run devnet:deploy`.
.zkuat-devnet.json
